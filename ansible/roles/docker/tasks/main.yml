# ansible/roles/docker/tasks/main.yml
---
- name: Check if Docker is installed
  command: docker --version
  register: docker_version_check
  failed_when: false
  changed_when: false

- name: Parse Docker version
  when: docker_version_check.rc == 0
  set_fact:
    docker_installed_version: "{{ docker_version_check.stdout | regex_search('\\d+\\.\\d+\\.\\d+') }}"

- name: Add Docker GPG key
  apt_key:
    url: https://download.docker.com/linux/{{ ansible_distribution | lower }}/gpg
    state: present
    keyring: /etc/apt/keyrings/docker.gpg

- name: Add Docker repository
  apt_repository:
    repo: >
      deb [arch={{ dpkg_architecture }}
      signed-by=/etc/apt/keyrings/docker.gpg]
      https://download.docker.com/linux/{{ ansible_distribution | lower }}
      {{ ansible_distribution_release }} stable
    state: present
    filename: docker
  vars:
    dpkg_architecture: "{{ lookup('pipe', 'dpkg --print-architecture') }}"

- name: Install Docker packages
  apt:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-buildx-plugin
      - docker-compose-plugin
    state: present
    update_cache: yes

- name: Ensure Docker service is running
  service:
    name: docker
    state: started
    enabled: yes

- name: Add users to docker group
  user:
    name: "{{ item }}"
    groups: docker
    append: yes
  loop: "{{ docker_users }}"

- name: Configure Docker daemon
  copy:
    content: |
      {
        "log-driver": "json-file",
        "log-opts": {
          "max-size": "10m",
          "max-file": "3"
        },
        "metrics-addr": "127.0.0.1:9323",
        "experimental": false
      }
    dest: /etc/docker/daemon.json
    mode: '0644'
  notify: restart docker

- name: Verify Docker installation
  command: docker run --rm hello-world
  changed_when: false
  register: docker_test

- name: Get Docker Compose version
  command: docker compose version
  register: compose_version_check
  changed_when: false

- name: Parse Docker Compose version
  set_fact:
    docker_compose_installed_version: "{{ compose_version_check.stdout | regex_search('v\\d+\\.\\d+\\.\\d+') }}"
