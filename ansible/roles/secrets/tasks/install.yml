---
# ansible/roles/secrets/tasks/install.yml
# Ensures the secret provider CLI is installed. No auth operations run.

- name: Validate configuration
  ansible.builtin.include_tasks: validate-config.yml

- name: Install provider CLI
  ansible.builtin.include_tasks: "adapters/{{ secret_provider }}.yml"
  vars:
    adapter_action: install
