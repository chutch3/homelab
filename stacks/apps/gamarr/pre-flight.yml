# DDL-only: no required env vars (GAMARR_RAWG_API_KEY is optional), so no validate block.

directories:
  - { path: gamarr, base: app_data, owner: 1000, group: 1000 }

templates:
  - { src: sources.json, dest: gamarr/sources.json, owner: 1000, group: 1000, mode: "0644" }
