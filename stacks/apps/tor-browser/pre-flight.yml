validate:
  env:
    - TOR_WIREGUARD_PRIVATE_KEY
    - TOR_WIREGUARD_ADDRESSES
    - TOR_BROWSER_VNC_PASSWORD
  labels:
    - tor: "true"

# Data dir on shared storage (created cluster-side by the preflight role).
directories:
  - { path: tor-browser, owner: 1000, group: 1000 }
